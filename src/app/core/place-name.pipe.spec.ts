import { PlaceNamePipe } from './place-name.pipe';

describe('PlaceNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceNamePipe();
    expect(pipe).toBeTruthy();
  });
});
